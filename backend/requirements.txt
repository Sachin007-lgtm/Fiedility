# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1

# PDF
PyMuPDF==1.24.5

# Retrieval
rank-bm25==0.2.2
sentence-transformers==3.0.1
chromadb==0.5.3
numpy==1.26.4

# LLM
groq==0.9.0

# Web search (existing)
duckduckgo-search==6.2.4

# Monitoring / eval
# (no extra deps needed — all stdlib + above)

# Dev / CI
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
ruff==0.5.0
